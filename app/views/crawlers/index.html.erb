<!DOCTYPE html>
<html>
<head>
  <title>Website Crawler</title>
  <style>
    body {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      background-color: #f5f5f5;
      font-family: Arial, sans-serif;
    }

    .container {
      text-align: center;
    }

    h1 {
      color: #333;
      font-size: 24px;
      margin-bottom: 20px;
    }

    form {
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .form-field {
      margin-bottom: 10px;
    }

    .form-field input[type="text"] {
      padding: 8px;
      font-size: 14px;
    }

    .form-field input[type="submit"] {
      padding: 10px 20px;
      font-size: 16px;
      color: #fff;
      background: linear-gradient(to right, #00bfff, #008000);
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }

    .flash {
      margin-top: 10px;
      padding: 10px;
      font-size: 14px;
      font-weight: bold;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Website Crawler</h1>
    <%= form_with url: '/crawl', method: :post do |form| %>
      <div class="form-field">
        <%= form.text_field :url, placeholder: 'Enter website URL', required: true %>
      </div>
      <div class="form-field">
        <%= form.submit 'Start Crawling' %>
      </div>
    <% end %>
    <% flash.each do |key, value| %>
      <div class="flash <%= key %>"><%= value %></div>
    <% end %>
  </div>
</body>
</html>
